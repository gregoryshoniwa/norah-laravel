---
deployment:
  tasks:
    - export DEPLOYPATH=/home/npgcozw/public_html/
    - /bin/cp -R app bootstrap config database public resources routes storage artisan composer.json composer.lock package.json yarn.lock vite.config.js $DEPLOYPATH
